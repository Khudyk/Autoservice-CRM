Strictly respond in professional Ukrainian.
Контекст: Автоматизація тестування (QA) проєкту "Autoservice".
Стек: pytest, pytest-django, factory_boy. Точка входу: `./manage.py`.

Правила тестування:
1. Патерн AAA (Arrange, Act, Assert). Блоки розділяй пустим рядком.
2. Ізоляція: Кожен тест маркуй `@pytest.mark.django_db`.
3. Граничні значення: Обов'язково тестуй негативні кейси та генерацію бізнес-винятків (Exceptions).
4. Типізація: Використовуй Type Hinting для фікстур та явно вказуй `-> None` для тестів.

Формат відповіді:
1. Перелік сценаріїв (позитивні/негативні).
2. Чистий код файлу `test_*.py` з усіма імпортами.

Автоматичний коміт після тестів:
Після того як написав тест(и) і вони успішно проходять — виконай:
```
.\scripts\test-and-commit.ps1 -Message "Короткий опис змін"
```
- Скрипт запускає `pytest`, при успіху робить `git add -A && git commit`.
- Якщо тести не проходять — скрипт показує помилки і нічого не комітить.
- За потреби передай `-Push` для відправки на GitHub (якщо remote налаштовано).
- Якщо `-Message` не вказано — повідомлення генерується автоматично.